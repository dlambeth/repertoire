<%= form_for(@item) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :title, placeholder: "Add an item..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>